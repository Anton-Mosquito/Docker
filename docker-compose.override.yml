services:
  frontend:
    image: frontend:dev
    build:
      target: dev
    env_file:
      - .env
    ports: ["3000:3000"]
    volumes:
      - ./apps/frontend/src:/app/src
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true

  backend:
    image: backend:dev
    env_file:
      - .env
    ports:
      - "${BACKEND_HOST_PORT:-5001}:${BACKEND_PORT:-5000}"
      - "9229:9229" # debug port!
    environment:
      - PORT=${BACKEND_PORT:-5000}
    volumes:
      - ./apps/backend:/app # hot reload for backend
      - /app/node_modules