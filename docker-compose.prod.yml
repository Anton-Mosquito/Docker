#  production overrides
services:
  frontend:
    ports: ["80:80"]
    restart: always

  backend:
    ports: ["5001:5000"]
    restart: always

# Launch prod config:
# docker compose -f docker-compose.yml -f docker-compose.prod.yml up
